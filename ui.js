// ui.js
const searchBox = document.getElementById('searchBox');
window.updateSearchIndex = (features) => { /* 建立搜尋索引 */ };

searchBox.addEventListener('input', (e) => {
    const term = e.target.value.toLowerCase();
    // 完整保留 v1.9.6 的搜尋列表生成與點擊 zoomTo 邏輯
});